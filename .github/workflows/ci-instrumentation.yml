name: "🧪 Instrumentation Test Matrix"

on:
  workflow_call:

jobs:

  instrumentation:
    name: "${{ matrix.api }}"

    uses: TWiStErRob/github-workflows/.github/workflows/instrumentation.yml@cb91b4b05800bd3de29bc0e63900f304a45fbf06 # v3
    with:
      android-api: ${{ matrix.api }}
      script: >
        ./gradlew
        --continue
        --stacktrace
        --scan
        connectedCheck
        mergeAndroidReports

    permissions:
      contents: read
      checks: write
      statuses: write

    strategy:
      fail-fast: false
      matrix:
        api:
          - 34
